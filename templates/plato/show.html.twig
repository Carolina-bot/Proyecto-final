{% extends 'base.html.twig' %}

{% block title %}Plato{% endblock %}

{% block body %}
    <h1>Plato</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plato.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ plato.Nombre }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ plato.Tipo }}</td>
            </tr>
            <tr>
                <th>Altranuces</th>
                <td>{{ plato.Altranuces ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Apio</th>
                <td>{{ plato.Apio ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Cacahuetes</th>
                <td>{{ plato.Cacahuetes ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Crustaceos</th>
                <td>{{ plato.Crustaceos ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Sulfitos</th>
                <td>{{ plato.Sulfitos ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Cascara</th>
                <td>{{ plato.Cascara ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Gluten</th>
                <td>{{ plato.Gluten ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Sesamo</th>
                <td>{{ plato.Sesamo ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Huevo</th>
                <td>{{ plato.Huevo ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Lacteos</th>
                <td>{{ plato.Lacteos ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Moluscos</th>
                <td>{{ plato.Moluscos ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Mostaza</th>
                <td>{{ plato.Mostaza ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Pescado</th>
                <td>{{ plato.Pescado ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Soja</th>
                <td>{{ plato.Soja ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plato_index') }}">back to list</a>

    <a href="{{ path('app_plato_edit', {'id': plato.id}) }}">edit</a>

    {{ include('plato/_delete_form.html.twig') }}
{% endblock %}
